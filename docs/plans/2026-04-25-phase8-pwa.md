# Phase 8 PWA Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** spec `docs/specs/2026-04-20-travel-manager-design-updated.md` §0/§3/§9 의 미완 항목 "Phase 8 PWA (Workbox 쉘 프리캐시 + manifest.json + sw.js)" 를 구현해 (a) 모바일 홈화면 설치 가능, (b) 재방용 시 정적 자산 즉시 로드, (c) 오프라인 fallback 노출 까지 완성한다. 마이크로 인터랙션 폴리시는 본 plan 외부 (별도 plan 후보).

**Architecture:** `@serwist/next` (Workbox 후속, Next 14+/15+/16 명시 지원) 의 `injectManifest` 모드 채택. 사용자가 `app/sw.ts` 를 직접 작성 → 도구가 빌드 시 `self.__SW_MANIFEST` 에 precache 매니페스트(빌드 해시 포함)를 주입. `next.config.ts` 를 `withSerwist(...)` 로 wrap. `app/layout.tsx` metadata 에 manifest 링크 + iOS 메타 추가. SW 등록은 `<ServiceWorkerRegistrar />` 클라이언트 컴포넌트가 production 한정 mount. 캐시 정책: precache (next/static, fonts) + runtime cache(StaleWhileRevalidate for HTML, CacheFirst for fonts). **Supabase REST/Realtime/auth 와 Maps API 는 캐시하지 않음** (RLS·세션·rate-limit·실시간성 충돌). 오프라인 = `/offline` 정적 페이지로 navigation fallback.

**Tech Stack:** Next.js 16 App Router, `@serwist/next@9.5.7`, `serwist@9.5.7` (Workbox-fork), TypeScript, Tailwind, Vitest, Playwright. (peer: `next>=14.0.0`, `react>=18.0.0`)

---

## Spec ↔ Plan Coverage

| Spec 항목 | 위치 | 본 plan Task |
|---|---|---|
| `public/manifest.json`, `public/sw.js`, Workbox 부재 | §0.1 표 row | Task 2 (manifest), Task 3 (serwist setup), Task 4 (sw.ts) |
| Phase 8 = "PWA (Workbox 쉘 프리캐시 + manifest.json + sw.js)" | §0 / §9 표 | Task 2 / 3 / 4 / 5 |
| Shell-First Principle "[1] 앱 쉘 즉시 렌더 (Skeleton or PWA 프리캐시)" | §3 | Task 4 (precache + runtime cache 정책) |
| Tech Stack "PWA — 앱 설치, 쉘 프리캐시 (Workbox)" | §1 | Task 2~4 |
| In Scope V1: PWA ⏳ | §2 표 | Task 1~9 전체 |
| iOS 홈화면 호환 (모바일-퍼스트, "앱 설치") | §1 + DESIGN.md §10 | Task 6 (Apple meta + 아이콘) |
| 마이크로 인터랙션 폴리시 (Phase 8 묶음) | §9 | **본 plan 외부** — Task 9 retrospective 에서 follow-up 으로 분리 명시 |

**Out of Plan (의도적):**
- Background Sync / IndexedDB 큐잉 mutation 재시도 — Supabase 토큰 갱신/Realtime 구독과 충돌 위험 큼, 별도 plan
- Push Notification — 별도 ADR + Supabase function 필요
- 마이크로 인터랙션 폴리시 — 시각/타이밍 주제, PWA 와 독립

---

## File Structure

| 경로 | 액션 | 책임 |
|---|---|---|
| `package.json` | Modify | `@serwist/next`, `serwist` 추가 (devDependencies) |
| `next.config.ts` | Modify | `withSerwist(nextConfig)` wrap + CSP `worker-src 'self'` 추가 |
| `app/sw.ts` | Create | Service Worker entry — `installSerwist({ precacheEntries: self.__SW_MANIFEST, runtimeCaching, navigationPreload: true, fallbacks: { entries: [{ url: "/offline", matcher: ({request}) => request.destination === "document" }] } })` |
| `lib/pwa/runtime-caching.ts` | Create | RuntimeCaching 정책 export — Pretendard CacheFirst, `_next/static` CacheFirst, HTML SWR, Supabase·Maps NetworkOnly |
| `public/manifest.webmanifest` | Create | Web App Manifest — name/short_name/start_url/display=standalone/theme_color=#f2f1ed/background_color/icons[5] |
| `public/icons/icon-192.png` | Create | App icon 192×192 (PNG, opaque, monogram "tm" or 캐릭터) |
| `public/icons/icon-512.png` | Create | App icon 512×512 |
| `public/icons/maskable-512.png` | Create | Maskable icon 512×512 (안전영역 80%) |
| `public/icons/apple-touch-icon-180.png` | Create | iOS 홈화면 아이콘 180×180 |
| `public/icons/icon.svg` | Create | 마스터 SVG (재생성 시 source) |
| `scripts/generate-pwa-icons.mjs` | Create | SVG → 5 PNG 변환 (Node `sharp`) — devDependency 추가 |
| `app/layout.tsx` | Modify | Metadata 에 `manifest: "/manifest.webmanifest"` + Apple meta + viewport themeColor 유지 |
| `app/offline/page.tsx` | Create | 오프라인 fallback 페이지 (Server Component, 정적) — 안내 문구 + 새로고침 버튼(client island) |
| `app/offline/loading.tsx` | (skip) | 정적이라 불필요 |
| `components/pwa/service-worker-registrar.tsx` | Create | `"use client"` — production 환경에서만 `<Serwist />` 동등 register 수행. dev/test 에선 noop |
| `app/providers.tsx` | Modify | `<ServiceWorkerRegistrar />` mount (RealtimeGateway 옆) |
| `tests/unit/pwa-manifest.test.ts` | Create | manifest schema (zod) + 필수 필드 + 아이콘 5개 + start_url=/ |
| `tests/unit/pwa-runtime-caching.test.ts` | Create | runtime caching 정책의 Supabase/Maps URL 매칭 → NetworkOnly 보장 |
| `tests/integration/pwa-build-artifacts.test.ts` | Create | `pnpm build` 후 `public/sw.js` (또는 next 빌드 산출물) + manifest 존재 검증 |
| `tests/e2e/pwa.spec.ts` | Create | E2E (anonymous project) — `/manifest.webmanifest` 200 + `Content-Type` 정상 + `/offline` 200 + production build 일 때 SW 등록 (`navigator.serviceWorker.controller`) |
| `playwright.config.ts` | Modify | `pwa.spec.ts` 를 anonymous project 에 추가 (인증 없음, 정적 자산 검증) |
| `docs/specs/2026-04-20-travel-manager-design-updated.md` | Modify | §0 status row Phase 8 ⏳→✅, §0.1 mapping row 갱신, 변경 이력 append |
| `docs/qa/phase8-pwa-checklist.md` | Create | Manual QA 체크리스트 — Chrome DevTools Application 탭, iOS Safari 홈화면 추가, 오프라인 토글 |

**Dependency 순서:** Task 1 (deps) → Task 2 (manifest + 아이콘) → Task 3 (next.config wrap + CSP) → Task 4 (sw.ts + runtime caching) → Task 5 (offline page) → Task 6 (layout metadata + Apple meta) → Task 7 (registrar + providers) → Task 8 (tests: unit + integration + E2E) → Task 9 (verification + spec/wiki + commit + tag).

---

## Conventions

- **Branch:** main 직접 (위크리 미사용 — 단일 commit 단위 small, 위험 낮음)
- **Commit prefix:** `feat(pwa):` / `chore(deps):` / `test(...):` / `docs(spec):`
- **Co-author footer:** `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`
- **Test 명명:** unit/integration `.test.ts`, E2E `.spec.ts`
- **테스트 위치:**
  - unit → `tests/unit/`
  - integration → `tests/integration/`
  - E2E → `tests/e2e/` + `playwright.config.ts` project testMatch 갱신
- **dev 환경 SW 비활성화 정책:** `next.config.ts` 의 `withSerwist({ disable: process.env.NODE_ENV === "development" })`. dev 에서 SW 동작 시 HMR 와 충돌 + Workbox console 노이즈 발생.
- **CSP 정책:** Service Worker 자체는 `worker-src 'self'` 필요. SW 가 fetch 하는 origin 은 기존 `script-src`/`connect-src` 와 동일 (변경 없음).

---

### Task 1: 의존성 추가 (`@serwist/next`, `serwist`, `sharp`)

**Files:**
- Modify: `package.json`
- Modify: `pnpm-lock.yaml` (자동 생성)

- [ ] **Step 1: 의존성 설치 (devDependencies)**

```bash
pnpm add -D @serwist/next@9.5.7 serwist@9.5.7 sharp@0.33.5
```

Expected:
- `package.json` devDependencies 에 3 항목 추가
- `pnpm-lock.yaml` 갱신
- 설치 시 `peer` 경고 0 (next 16 ≥ 14 만족)

- [ ] **Step 2: 설치 검증**

Run: `pnpm tsc --noEmit`
Expected: 0 errors (구현 전이므로 타입 import 변화 없음, 단순 dependency 추가).

Run: `pnpm lint`
Expected: 0 errors (warn 11건 사전 존재 OK).

- [ ] **Step 3: Commit**

```bash
git add package.json pnpm-lock.yaml
git commit -m "$(cat <<'EOF'
chore(deps): add @serwist/next + serwist + sharp for Phase 8 PWA

@serwist/next@9.5.7 (Workbox-based, Next 14+/15+/16 지원).
sharp 는 SVG → PWA 아이콘 PNG 5종 빌드용 (Task 2).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: PWA 아이콘 + manifest.webmanifest

**Files:**
- Create: `public/icons/icon.svg`
- Create: `scripts/generate-pwa-icons.mjs`
- Create: `public/icons/icon-192.png`
- Create: `public/icons/icon-512.png`
- Create: `public/icons/maskable-512.png`
- Create: `public/icons/apple-touch-icon-180.png`
- Create: `public/manifest.webmanifest`

- [ ] **Step 1: 마스터 SVG 생성**

`public/icons/icon.svg` 작성. 단순한 모노그램 (배경 #f2f1ed surface-200, 글자 #1a1a1a ink-900). 추후 디자이너 교체 가능.

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="512" height="512" viewBox="0 0 512 512">
  <rect width="512" height="512" rx="96" fill="#f2f1ed"/>
  <text x="50%" y="50%" font-family="Pretendard, system-ui, sans-serif"
        font-size="220" font-weight="700" fill="#1a1a1a"
        text-anchor="middle" dominant-baseline="central">tm</text>
</svg>
```

- [ ] **Step 2: 아이콘 생성 스크립트**

`scripts/generate-pwa-icons.mjs`:

```js
#!/usr/bin/env node
import sharp from "sharp";
import { mkdir } from "node:fs/promises";
import { resolve } from "node:path";

const SRC = resolve("public/icons/icon.svg");
const OUT_DIR = resolve("public/icons");

const TARGETS = [
  { size: 192, name: "icon-192.png", maskable: false },
  { size: 512, name: "icon-512.png", maskable: false },
  { size: 512, name: "maskable-512.png", maskable: true },
  { size: 180, name: "apple-touch-icon-180.png", maskable: false },
];

await mkdir(OUT_DIR, { recursive: true });

for (const { size, name, maskable } of TARGETS) {
  // maskable 은 안전영역 80% 위해 inner 80% 영역에 컨텐츠가 들어가도록 sharp 의
  // resize 후 extend 로 padding. 마스터 SVG 자체는 정사각 풀스케일이라 동일 SVG 를
  // 80% 크기로 줄여서 padding 추가.
  const innerSize = maskable ? Math.round(size * 0.8) : size;
  const pad = Math.round((size - innerSize) / 2);
  await sharp(SRC, { density: 600 })
    .resize(innerSize, innerSize, { fit: "contain", background: "#f2f1ed" })
    .extend({ top: pad, bottom: pad, left: pad, right: pad, background: "#f2f1ed" })
    .png({ compressionLevel: 9 })
    .toFile(resolve(OUT_DIR, name));
  console.log(`✓ ${name} (${size}×${size})`);
}
```

- [ ] **Step 3: package.json 스크립트 추가**

`package.json` `scripts` 블록에 추가:

```json
"icons:gen": "node scripts/generate-pwa-icons.mjs"
```

- [ ] **Step 4: 아이콘 생성 실행**

```bash
pnpm icons:gen
```

Expected:
- `public/icons/icon-192.png`
- `public/icons/icon-512.png`
- `public/icons/maskable-512.png`
- `public/icons/apple-touch-icon-180.png`
- 콘솔에 `✓ icon-192.png (192×192)` 등 4줄

- [ ] **Step 5: manifest.webmanifest 작성**

`public/manifest.webmanifest`:

```json
{
  "name": "travel-manager",
  "short_name": "trip-mgr",
  "description": "커플·소그룹 여행 플래너",
  "start_url": "/",
  "scope": "/",
  "display": "standalone",
  "orientation": "portrait",
  "background_color": "#f2f1ed",
  "theme_color": "#f2f1ed",
  "lang": "ko",
  "categories": ["travel", "productivity"],
  "icons": [
    { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png", "purpose": "any" },
    { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any" },
    { "src": "/icons/maskable-512.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable" }
  ]
}
```

> **Note:** `apple-touch-icon-180.png` 는 manifest 가 아니라 `<link rel="apple-touch-icon">` 로 노출 (Task 6).

- [ ] **Step 6: 검증 — manifest 정적 응답**

```bash
pnpm dev &
DEV_PID=$!
sleep 6
curl -sI http://localhost:3000/manifest.webmanifest | head -5
kill $DEV_PID 2>/dev/null
```

Expected:
- `HTTP/1.1 200 OK`
- `Content-Type: application/manifest+json` (Next 가 .webmanifest 확장자에 자동 매핑) **또는** `application/json` — 둘 다 허용 (Chrome 양쪽 인식)

- [ ] **Step 7: Commit**

```bash
git add public/icons/ public/manifest.webmanifest scripts/generate-pwa-icons.mjs package.json
git commit -m "$(cat <<'EOF'
feat(pwa): add web app manifest + 4 icons (192/512/maskable/apple)

마스터 SVG 1개 + sharp 변환 스크립트로 빌드 시 재생성 가능.
manifest.webmanifest 는 Next 가 정적 파일로 서빙. theme_color 는
layout.tsx 의 viewport.themeColor 와 동기 (#f2f1ed).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: `next.config.ts` 에 Serwist wrap + CSP 갱신

**Files:**
- Modify: `next.config.ts`

- [ ] **Step 1: 현재 next.config.ts 읽기**

```bash
cat next.config.ts
```

Expected: `nextConfig: NextConfig` export, `headers()` 에 보안 헤더 + CSP. import 는 `import type { NextConfig } from "next"` 1줄.

- [ ] **Step 2: import 추가 + CSP 에 worker-src 추가**

`next.config.ts` 상단 import 블록 끝에 추가:

```ts
import withSerwistInit from "@serwist/next";
```

`securityHeaders` 배열의 CSP value 배열 (현재 `[ "default-src 'self'", \`script-src ${scriptSrc}\`, ... ].join("; ")`) 에서 `"frame-ancestors 'none'"` **앞에** 한 줄 추가:

```ts
"worker-src 'self'",
```

> **Why:** Service Worker 등록 시 브라우저가 `worker-src` 디렉티브를 평가. `script-src` 가 fallback 이지만 명시 권장 (Lighthouse PWA audit).

- [ ] **Step 3: serwist init 객체 정의 + export 변경**

파일 맨 아래 `export default nextConfig;` 를 다음으로 교체:

```ts
const withSerwist = withSerwistInit({
  swSrc: "app/sw.ts",
  swDest: "public/sw.js",
  cacheOnNavigation: true,
  reloadOnOnline: true,
  // dev SW 는 HMR 충돌 + Workbox console 노이즈 → 비활성화. preview/prod 만 동작.
  disable: process.env.NODE_ENV === "development",
});

export default withSerwist(nextConfig);
```

- [ ] **Step 4: 빌드 smoke (sw.ts 가 아직 없으므로 일부러 fail 확인 후 다음 task 로)**

Run: `pnpm build`
Expected:
- 에러 메시지에 `Cannot find module .../app/sw.ts` 또는 `swSrc not found` 등장 — Task 4 에서 sw.ts 생성 후 사라짐.

> **NOTE:** 이 step 은 의도적 fail. 다음 commit 까지 묶음.

- [ ] **Step 5: Commit (다음 task 와 동일 단위로 묶지 않고 독립 commit)**

```bash
git add next.config.ts
git commit -m "$(cat <<'EOF'
chore(next-config): wrap with @serwist/next + add worker-src CSP

withSerwist 가 swSrc=app/sw.ts → swDest=public/sw.js 빌드.
dev 는 disable=true 로 SW 비활성화 (HMR 충돌 회피).
worker-src 'self' 추가로 SW 등록 시 CSP 위반 차단.

Build 는 app/sw.ts 부재로 의도적 fail — 다음 commit 에서 sw.ts 추가.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: SW entry (`app/sw.ts`) + runtime caching policy + unit test (TDD)

**Files:**
- Create: `lib/pwa/runtime-caching.ts`
- Create: `app/sw.ts`
- Create: `tests/unit/pwa-runtime-caching.test.ts`

- [ ] **Step 1: Write failing unit test for runtime caching policy**

`tests/unit/pwa-runtime-caching.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { matchPolicy, RUNTIME_CACHE_POLICIES } from "@/lib/pwa/runtime-caching";

describe("runtime caching policy", () => {
  it("Supabase REST URL matches NetworkOnly", () => {
    const url = new URL("https://abc.supabase.co/rest/v1/trips?select=*");
    const policy = matchPolicy(url);
    expect(policy?.handler).toBe("NetworkOnly");
  });

  it("Supabase Realtime URL (wss) matches NetworkOnly", () => {
    const url = new URL("wss://abc.supabase.co/realtime/v1/websocket");
    const policy = matchPolicy(url);
    expect(policy?.handler).toBe("NetworkOnly");
  });

  it("Supabase auth callback matches NetworkOnly", () => {
    const url = new URL("https://abc.supabase.co/auth/v1/token");
    const policy = matchPolicy(url);
    expect(policy?.handler).toBe("NetworkOnly");
  });

  it("Naver Maps SDK matches NetworkOnly", () => {
    const url = new URL("https://oapi.map.naver.com/openapi/v3/maps.js");
    const policy = matchPolicy(url);
    expect(policy?.handler).toBe("NetworkOnly");
  });

  it("Google Maps loader matches NetworkOnly", () => {
    const url = new URL("https://maps.googleapis.com/maps/api/js");
    const policy = matchPolicy(url);
    expect(policy?.handler).toBe("NetworkOnly");
  });

  it("Pretendard font CSS matches CacheFirst", () => {
    const url = new URL("https://example.test/_next/static/media/Pretendard-Regular.woff2");
    const policy = matchPolicy(url);
    expect(policy?.handler).toBe("CacheFirst");
    expect(policy?.cacheName).toBe("static-assets");
  });

  it("HTML navigation request matches StaleWhileRevalidate", () => {
    const url = new URL("https://example.test/trips/abc");
    const policy = matchPolicy(url, "document");
    expect(policy?.handler).toBe("StaleWhileRevalidate");
    expect(policy?.cacheName).toBe("pages");
  });

  it("Unknown 3rd party returns null (default to NetworkOnly via SW config)", () => {
    const url = new URL("https://random.example.com/foo");
    const policy = matchPolicy(url);
    expect(policy).toBeNull();
  });

  it("RUNTIME_CACHE_POLICIES exports a non-empty array", () => {
    expect(RUNTIME_CACHE_POLICIES.length).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run tests/unit/pwa-runtime-caching.test.ts`
Expected: FAIL with "Cannot find module '@/lib/pwa/runtime-caching'"

- [ ] **Step 3: Implement runtime caching policy module**

`lib/pwa/runtime-caching.ts`:

```ts
/**
 * @serwist/next runtime caching 정책.
 *
 * 핵심 설계:
 *  - Supabase / Maps API / Google auth 는 **절대 캐시 안 함** (RLS·세션·rate-limit·실시간성).
 *  - 정적 자산 (`/_next/static/*`, font woff2) 은 CacheFirst (해시 기반 immutable 자산).
 *  - HTML navigation 은 SWR — 즉시 캐시 응답, background 갱신.
 *
 * 본 모듈은 SW 빌드 시점 (Workbox routing 등록) + unit test 양쪽에서 import.
 */

export type CacheHandler = "NetworkOnly" | "CacheFirst" | "StaleWhileRevalidate";

export type RuntimeCachePolicy = {
  /** 매칭 대상 origin host suffix 또는 pathname prefix */
  match: (url: URL, destination?: RequestDestination) => boolean;
  handler: CacheHandler;
  cacheName?: string;
  /** 표시용 라벨 (디버깅/로그) */
  label: string;
};

const SUPABASE_HOST_SUFFIX = ".supabase.co";
const NAVER_MAPS_SUFFIXES = [
  ".map.naver.com",
  ".pstatic.net",
  ".map.naver.net",
  "naveropenapi.apigw.ntruss.com",
];
const GOOGLE_API_HOSTS = [
  "maps.googleapis.com",
  "maps.gstatic.com",
  "accounts.google.com",
];

export const RUNTIME_CACHE_POLICIES: RuntimeCachePolicy[] = [
  {
    label: "supabase-rest-realtime-auth",
    handler: "NetworkOnly",
    match: (url) => url.host.endsWith(SUPABASE_HOST_SUFFIX),
  },
  {
    label: "naver-maps",
    handler: "NetworkOnly",
    match: (url) =>
      NAVER_MAPS_SUFFIXES.some((s) => url.host === s || url.host.endsWith(s)),
  },
  {
    label: "google-maps-and-auth",
    handler: "NetworkOnly",
    match: (url) => GOOGLE_API_HOSTS.includes(url.host),
  },
  {
    label: "next-static-assets",
    handler: "CacheFirst",
    cacheName: "static-assets",
    // /_next/static/** + font woff2/woff/ttf 정적 미디어
    match: (url) =>
      url.pathname.startsWith("/_next/static/") ||
      /\.(?:woff2?|ttf|otf)$/i.test(url.pathname),
  },
  {
    label: "html-pages",
    handler: "StaleWhileRevalidate",
    cacheName: "pages",
    match: (_url, destination) => destination === "document",
  },
];

export function matchPolicy(
  url: URL,
  destination?: RequestDestination,
): RuntimeCachePolicy | null {
  for (const p of RUNTIME_CACHE_POLICIES) {
    if (p.match(url, destination)) return p;
  }
  return null;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm vitest run tests/unit/pwa-runtime-caching.test.ts`
Expected: 9 passed.

- [ ] **Step 5: Implement `app/sw.ts`**

> **Adaptation guidance (Serwist 9.5.7 API):**
> 본 plan 작성 시 Serwist 공식 docs (https://serwist.pages.dev) 접근 불가 상태였음.
> 아래 코드는 9.x 의 일반 패턴이지만, **implementer 가 실제 빌드 에러 발생 시
> 공식 docs / `node_modules/serwist/dist/index.d.ts` 의 export 시그니처로 확인 후 적응할 것.**
> 핵심 의도:
>   1. precacheEntries 는 `self.__SW_MANIFEST` (도구 주입)
>   2. runtimeCaching matcher 는 우리 정책 (Supabase/Maps NetworkOnly) 가 default 보다 **앞에**
>   3. fallback 은 navigation 실패 → `/offline` 라우트
>   4. install 시 skipWaiting + activate 시 clientsClaim
> 이 4 가지가 보장되면 import 명/클래스명/메서드명 차이는 자유롭게 적응.

`app/sw.ts`:

```ts
/// <reference lib="webworker" />
import { defaultCache } from "@serwist/next/worker";
import type { PrecacheEntry, SerwistGlobalConfig } from "serwist";
import { Serwist } from "serwist";
import {
  CacheFirst,
  NetworkOnly,
  StaleWhileRevalidate,
} from "serwist/strategies";
import { RUNTIME_CACHE_POLICIES } from "@/lib/pwa/runtime-caching";

declare global {
  interface WorkerGlobalScope extends SerwistGlobalConfig {
    __SW_MANIFEST: (PrecacheEntry | string)[] | undefined;
  }
}

declare const self: ServiceWorkerGlobalScope;

const strategyFor = (handler: string, cacheName?: string) => {
  switch (handler) {
    case "CacheFirst":
      return new CacheFirst({ cacheName });
    case "StaleWhileRevalidate":
      return new StaleWhileRevalidate({ cacheName });
    case "NetworkOnly":
    default:
      return new NetworkOnly();
  }
};

const runtimeCaching = RUNTIME_CACHE_POLICIES.map((p) => ({
  matcher: ({ url, request }: { url: URL; request: Request }) =>
    p.match(url, request.destination as RequestDestination | undefined),
  handler: strategyFor(p.handler, p.cacheName),
  method: "GET" as const,
}));

const serwist = new Serwist({
  precacheEntries: self.__SW_MANIFEST,
  skipWaiting: true,
  clientsClaim: true,
  navigationPreload: true,
  runtimeCaching: [...runtimeCaching, ...defaultCache],
  fallbacks: {
    entries: [
      {
        url: "/offline",
        matcher: ({ request }) => request.destination === "document",
      },
    ],
  },
});

serwist.addEventListeners();
```

> **Why `defaultCache` 다음에 우리 정책?** runtime cache matcher 는 첫 매칭 우선. 우리 NetworkOnly (Supabase/Maps) 가 먼저 매칭돼야 함. `defaultCache` 는 일반적 정적 자산 fallback 역할.

- [ ] **Step 6: Build smoke**

Run: `pnpm build`
Expected:
- 에러 0
- 출력에 `[Serwist]` 또는 `Service worker built` 메시지
- `public/sw.js` 가 생성됨 (확장자 `.js`, 크기 50KB+)

```bash
ls -la public/sw.js
```
Expected: 파일 존재.

- [ ] **Step 7: `.gitignore` 갱신 (sw.js 는 빌드 산출물이라 commit 금지)**

`.gitignore` "tests / coverage artifacts" 블록 위에 추가:

```
# PWA — service worker 는 next build 산출물 (manifest 는 commit, sw.js 는 ignore)
/public/sw.js
/public/sw.js.map
/public/swe-worker-*.js
```

- [ ] **Step 8: Commit**

```bash
git add lib/pwa/runtime-caching.ts app/sw.ts tests/unit/pwa-runtime-caching.test.ts .gitignore
git commit -m "$(cat <<'EOF'
feat(pwa): service worker entry + runtime cache policy (Workbox/Serwist)

- lib/pwa/runtime-caching.ts: Supabase/Naver/Google = NetworkOnly,
  /_next/static + font = CacheFirst, HTML = StaleWhileRevalidate
- app/sw.ts: Serwist 인스턴스 — precache(self.__SW_MANIFEST)
  + runtimeCaching + /offline navigation fallback
- tests/unit/pwa-runtime-caching.test.ts: 9 cases (Supabase/Maps NetworkOnly
  보장 + 정적 자산 CacheFirst + HTML SWR)
- .gitignore: public/sw.js (빌드 산출물)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: 오프라인 fallback 페이지

**Files:**
- Create: `app/offline/page.tsx`
- Create: `app/offline/refresh-button.tsx`

- [ ] **Step 1: 오프라인 페이지 (Server Component)**

`app/offline/page.tsx`:

```tsx
import type { Metadata } from "next";
import { RefreshButton } from "./refresh-button";

export const metadata: Metadata = {
  title: "오프라인 — travel-manager",
  robots: { index: false, follow: false },
};

export default function OfflinePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-6 px-6 text-center">
      <div className="text-6xl" aria-hidden>
        ✈️
      </div>
      <h1 className="text-2xl font-semibold text-ink-900">
        오프라인 상태예요
      </h1>
      <p className="max-w-xs text-sm leading-6 text-ink-700">
        인터넷 연결이 끊어졌어요. 이미 본 일정은 계속 보이지만,
        새 정보를 불러오려면 연결을 확인해주세요.
      </p>
      <RefreshButton />
    </main>
  );
}
```

- [ ] **Step 2: refresh button (Client island)**

`app/offline/refresh-button.tsx`:

```tsx
"use client";

export function RefreshButton() {
  return (
    <button
      type="button"
      onClick={() => window.location.reload()}
      className="rounded-full bg-ink-900 px-6 py-3 text-sm font-medium text-surface-200 transition-opacity hover:opacity-90"
    >
      다시 시도
    </button>
  );
}
```

- [ ] **Step 3: Build smoke**

Run: `pnpm build`
Expected: 출력에 `/offline ○ Static` row 추가 (15 → 16 routes).

- [ ] **Step 4: Visual smoke (dev)**

```bash
pnpm dev &
DEV_PID=$!
sleep 6
curl -s http://localhost:3000/offline | grep -o "오프라인 상태예요"
kill $DEV_PID 2>/dev/null
```

Expected: `오프라인 상태예요` 출력.

- [ ] **Step 5: Commit**

```bash
git add app/offline/
git commit -m "$(cat <<'EOF'
feat(pwa): /offline fallback page (static, navigation fallback target)

Server Component + 'use client' refresh button island.
SW 의 fallbacks.entries 가 navigation 실패 시 이 페이지로 라우팅.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: `app/layout.tsx` metadata + iOS Apple meta

**Files:**
- Modify: `app/layout.tsx`

- [ ] **Step 1: 현재 layout.tsx 의 `metadata` / `viewport` 확인**

```bash
sed -n '1,35p' app/layout.tsx
```

Expected:
- `metadata.title = "travel-manager (mockup)"`
- `viewport.themeColor = "#f2f1ed"`
- manifest 링크 없음, apple-touch-icon 없음

- [ ] **Step 2: metadata 갱신**

`app/layout.tsx` 의 `metadata` 객체를 다음으로 교체:

```tsx
export const metadata: Metadata = {
  title: {
    default: "travel-manager",
    template: "%s — travel-manager",
  },
  description: "커플·소그룹 여행 플래너 PWA",
  applicationName: "travel-manager",
  manifest: "/manifest.webmanifest",
  appleWebApp: {
    capable: true,
    statusBarStyle: "default",
    title: "travel-manager",
  },
  icons: {
    icon: [
      { url: "/icons/icon-192.png", sizes: "192x192", type: "image/png" },
      { url: "/icons/icon-512.png", sizes: "512x512", type: "image/png" },
    ],
    apple: [{ url: "/icons/apple-touch-icon-180.png", sizes: "180x180" }],
  },
  robots: { index: false, follow: false },
};
```

> **Note:** `title.default` 가 `(mockup)` 접미사를 제거 — Phase 0 잔존. 변경 후 다른 페이지 metadata 가 깨지는지 build smoke 에서 확인.

- [ ] **Step 3: viewport 는 그대로 유지**

확인만: `viewport.themeColor === "#f2f1ed"` 가 manifest theme_color 와 일치.

- [ ] **Step 4: Build + 검증**

Run: `pnpm build`
Expected:
- 에러 0
- output HTML 에 `<link rel="manifest" href="/manifest.webmanifest">` + `<link rel="apple-touch-icon" sizes="180x180" href="/icons/apple-touch-icon-180.png">` 자동 주입 (Next metadata API)

- [ ] **Step 5: dev smoke 로 head 검사**

```bash
pnpm dev &
DEV_PID=$!
sleep 6
curl -s http://localhost:3000 | grep -E "manifest\.webmanifest|apple-touch-icon|application-name"
kill $DEV_PID 2>/dev/null
```

Expected: 3 줄 모두 매칭.

- [ ] **Step 6: Commit**

```bash
git add app/layout.tsx
git commit -m "$(cat <<'EOF'
feat(pwa): wire manifest + Apple touch icon + applicationName meta

Next metadata API 가 자동으로 <link rel="manifest"> +
<link rel="apple-touch-icon"> + <meta name="apple-mobile-web-app-*"> 주입.
title 의 (mockup) 접미사 제거 — Phase 0 잔존.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: SW 등록 컴포넌트 + providers 배선

**Files:**
- Create: `components/pwa/service-worker-registrar.tsx`
- Modify: `app/providers.tsx`

- [ ] **Step 1: 등록 컴포넌트 작성**

`components/pwa/service-worker-registrar.tsx`:

```tsx
"use client";

import { useEffect } from "react";

/**
 * Service Worker 등록 — production 에서만 동작.
 *
 * @serwist/next 가 빌드 시점에 public/sw.js 를 생성하지만,
 * 실제 등록 (`navigator.serviceWorker.register`) 은 런타임 코드가 호출해야 함.
 *
 * 정책:
 *  - process.env.NODE_ENV !== "production" → noop (dev/test 에선 SW 비활성화)
 *  - 미지원 브라우저 → noop (typeof navigator.serviceWorker === "undefined")
 *  - 등록 실패 → console.warn 후 noop (PWA 미설치 상태로 우아하게 폴백)
 */
export function ServiceWorkerRegistrar() {
  useEffect(() => {
    if (process.env.NODE_ENV !== "production") return;
    if (typeof navigator === "undefined" || !("serviceWorker" in navigator)) {
      return;
    }
    const onLoad = () => {
      navigator.serviceWorker
        .register("/sw.js", { scope: "/" })
        .catch((err) => {
          console.warn("[pwa] SW registration failed:", err);
        });
    };
    if (document.readyState === "complete") {
      onLoad();
    } else {
      window.addEventListener("load", onLoad, { once: true });
      return () => window.removeEventListener("load", onLoad);
    }
  }, []);
  return null;
}
```

- [ ] **Step 2: providers.tsx 에 mount**

`app/providers.tsx` 를 다음으로 교체:

```tsx
"use client";

import type { ReactNode } from "react";
import { QueryProvider } from "@/lib/query/provider";
import { RealtimeGateway } from "@/components/realtime/realtime-gateway";
import { ServiceWorkerRegistrar } from "@/components/pwa/service-worker-registrar";

export function Providers({ children }: { children: ReactNode }) {
  return (
    <QueryProvider>
      <RealtimeGateway />
      <ServiceWorkerRegistrar />
      {children}
    </QueryProvider>
  );
}
```

- [ ] **Step 3: lint + tsc**

Run: `pnpm tsc --noEmit && pnpm lint`
Expected: 0 errors.

- [ ] **Step 4: Build smoke (production 모드 — SW 활성)**

Run: `pnpm build`
Expected: 에러 0, `public/sw.js` 갱신.

- [ ] **Step 5: production 서버 smoke**

```bash
pnpm build && pnpm start &
PROD_PID=$!
sleep 8
# SW 가 등록되는지 확인 — register 호출은 클라이언트 JS 로 발생하므로 직접 검증 어려움
# 대신 sw.js 가 GET 200 + Service-Worker-Allowed 헤더 OK 확인
curl -sI http://localhost:3000/sw.js | head -3
kill $PROD_PID 2>/dev/null
```

Expected: `HTTP/1.1 200 OK` + Content-Type `application/javascript`.

- [ ] **Step 6: Commit**

```bash
git add components/pwa/ app/providers.tsx
git commit -m "$(cat <<'EOF'
feat(pwa): register service worker via Providers (production-only)

ServiceWorkerRegistrar 가 production NODE_ENV 에서만 sw.js register.
dev/test 는 noop (HMR 충돌 회피).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: Tests — manifest unit + build artifacts integration + E2E

**Files:**
- Create: `tests/unit/pwa-manifest.test.ts`
- Create: `tests/integration/pwa-build-artifacts.test.ts`
- Create: `tests/e2e/pwa.spec.ts`
- Modify: `playwright.config.ts`

- [ ] **Step 1: Unit test — manifest schema (TDD: 작성 → 통과)**

`tests/unit/pwa-manifest.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { readFileSync } from "node:fs";
import { resolve } from "node:path";
import { z } from "zod";

const ManifestSchema = z.object({
  name: z.string().min(1),
  short_name: z.string().min(1).max(12),
  description: z.string().min(1),
  start_url: z.string().startsWith("/"),
  scope: z.string().startsWith("/"),
  display: z.enum(["standalone", "fullscreen", "minimal-ui", "browser"]),
  background_color: z.string().regex(/^#[0-9a-fA-F]{6}$/),
  theme_color: z.string().regex(/^#[0-9a-fA-F]{6}$/),
  lang: z.string(),
  icons: z
    .array(
      z.object({
        src: z.string(),
        sizes: z.string(),
        type: z.string(),
        purpose: z.enum(["any", "maskable", "monochrome"]).optional(),
      }),
    )
    .min(2),
});

describe("PWA manifest", () => {
  const manifest = JSON.parse(
    readFileSync(resolve("public/manifest.webmanifest"), "utf-8"),
  );

  it("matches schema", () => {
    expect(() => ManifestSchema.parse(manifest)).not.toThrow();
  });

  it("includes a maskable 512×512 icon", () => {
    const maskable = manifest.icons.find(
      (i: { sizes: string; purpose?: string }) =>
        i.sizes === "512x512" && i.purpose === "maskable",
    );
    expect(maskable).toBeDefined();
  });

  it("theme_color matches viewport themeColor in app/layout.tsx", () => {
    const layout = readFileSync(resolve("app/layout.tsx"), "utf-8");
    const m = layout.match(/themeColor:\s*["']([^"']+)["']/);
    expect(m?.[1]).toBe(manifest.theme_color);
  });

  it("start_url and scope are root", () => {
    expect(manifest.start_url).toBe("/");
    expect(manifest.scope).toBe("/");
  });
});
```

- [ ] **Step 2: Run unit test**

Run: `pnpm vitest run tests/unit/pwa-manifest.test.ts`
Expected: 4 passed.

- [ ] **Step 3: Integration test — build artifacts**

`tests/integration/pwa-build-artifacts.test.ts`:

```ts
import { describe, it, expect, beforeAll } from "vitest";
import { existsSync, statSync } from "node:fs";
import { resolve } from "node:path";
import { execSync } from "node:child_process";

describe("PWA build artifacts", () => {
  beforeAll(() => {
    // 이미 빌드돼 있으면 skip — full integration suite 가 build 매번 돌리면 비싸므로
    // 사전 조건: pnpm build 가 한 번 이라도 실행됐어야 함.
    if (!existsSync(resolve(".next/BUILD_ID"))) {
      console.warn("[pwa-build-artifacts] running pnpm build (one-time)");
      execSync("pnpm build", { stdio: "inherit" });
    }
  }, 180_000);

  it("public/sw.js exists and is non-trivial", () => {
    const path = resolve("public/sw.js");
    expect(existsSync(path)).toBe(true);
    expect(statSync(path).size).toBeGreaterThan(10_000); // Workbox shell ≥ 10KB
  });

  it("public/manifest.webmanifest exists", () => {
    expect(existsSync(resolve("public/manifest.webmanifest"))).toBe(true);
  });

  it("4 icons exist", () => {
    for (const f of [
      "icon-192.png",
      "icon-512.png",
      "maskable-512.png",
      "apple-touch-icon-180.png",
    ]) {
      expect(existsSync(resolve("public/icons", f))).toBe(true);
    }
  });
});
```

- [ ] **Step 4: Run integration test**

Run: `pnpm vitest run --config vitest.integration.config.ts tests/integration/pwa-build-artifacts.test.ts`
Expected: 3 passed (첫 실행 시 build 약 60s 추가).

> **Why not `pnpm test:integration <path>`?** package.json 의 `test:integration` 은 `vitest run --config vitest.integration.config.ts` 로 정의돼 있어 path 추가 인자 전달 시 pnpm 가 인식 못 할 수 있음. CLI 직접 호출이 안전.

- [ ] **Step 5: E2E test — manifest + offline + sw.js**

`tests/e2e/pwa.spec.ts`:

```ts
import { test, expect } from "@playwright/test";

test.describe("PWA static assets", () => {
  test("/manifest.webmanifest serves valid JSON", async ({ request }) => {
    const res = await request.get("/manifest.webmanifest");
    expect(res.status()).toBe(200);
    const ct = res.headers()["content-type"] ?? "";
    expect(ct).toMatch(/manifest\+json|application\/json/);
    const body = await res.json();
    expect(body.name).toBe("travel-manager");
    expect(body.start_url).toBe("/");
    expect(Array.isArray(body.icons)).toBe(true);
  });

  test("/offline 페이지가 200 + 안내 문구 포함", async ({ page }) => {
    await page.goto("/offline");
    await expect(page.getByRole("heading", { name: "오프라인 상태예요" })).toBeVisible();
    await expect(page.getByRole("button", { name: "다시 시도" })).toBeVisible();
  });

  test("apple-touch-icon link 가 head 에 노출", async ({ page }) => {
    await page.goto("/");
    const href = await page
      .locator('link[rel="apple-touch-icon"]')
      .getAttribute("href");
    expect(href).toBe("/icons/apple-touch-icon-180.png");
  });

  test("manifest link 가 head 에 노출", async ({ page }) => {
    await page.goto("/");
    const href = await page
      .locator('link[rel="manifest"]')
      .getAttribute("href");
    expect(href).toBe("/manifest.webmanifest");
  });
});
```

- [ ] **Step 6: playwright.config.ts 갱신**

`playwright.config.ts` 의 `anonymous` project `testMatch` 배열을 다음으로 교체:

```ts
testMatch: ["login.spec.ts", "pwa.spec.ts"],
```

- [ ] **Step 7: E2E 실행**

Run: `pnpm test:e2e --project anonymous`
Expected: login.spec + pwa.spec 합쳐서 모두 PASS (pwa 4 cases).

> **Note:** dev mode 에서는 SW 가 disable=true 로 등록 안 됨. webServer 가 `pnpm dev` 라 manifest/offline 정적 응답만 검증 (SW 등록 자체는 production smoke 또는 manual QA 로).

- [ ] **Step 8: Full unit + integration 실행 (regression)**

Run: `pnpm test`
Expected: 기존 104 + 9 (runtime caching) + 4 (manifest) = 117 PASS.

Run: `pnpm test:integration`
Expected: 기존 + 3 (pwa-build-artifacts) PASS.

- [ ] **Step 9: Commit**

```bash
git add tests/unit/pwa-manifest.test.ts tests/integration/pwa-build-artifacts.test.ts tests/e2e/pwa.spec.ts playwright.config.ts
git commit -m "$(cat <<'EOF'
test(pwa): manifest schema + build artifacts + E2E (manifest/offline/icons)

unit 4: manifest schema · maskable icon · theme_color sync · start_url
integration 3: sw.js + manifest.webmanifest + 4 icons 빌드 산출물 존재
E2E 4: manifest 200/JSON · /offline · apple-touch-icon link · manifest link

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: Verification + spec/wiki 갱신 + tag

**Files:**
- Modify: `docs/specs/2026-04-20-travel-manager-design-updated.md`
- Create: `docs/qa/phase8-pwa-checklist.md`
- (위키) `~/Library/CloudStorage/SynologyDrive-home/앱/MY_AI_WIKI/projects/travel-manager/status.md` — Phase 8 ✅ row append

- [ ] **Step 1: 자동 게이트 전수 통과 확인**

Run (병렬 가능):

```bash
pnpm tsc --noEmit
pnpm lint
pnpm test
pnpm test:integration
pnpm build
pnpm test:e2e --project anonymous
pnpm test:e2e --project alice
```

Expected:
- tsc 0 / lint 0 errors
- unit: 기존 + 13 신규 (runtime-caching 9 + manifest 4) PASS
- integration: 기존 + 3 신규 (build artifacts) PASS
- build: 기존 15 + `/offline ○ Static` = 16 routes
- E2E anonymous: login 1 + pwa 4 = 5 PASS
- E2E alice: 12 기존 모두 PASS (regression 0)

> **Note:** 정확한 카운트는 implementer 가 실측. 본 plan 작성 시점의 기존 카운트:
> unit 104, integration 132 (sessions/2026-04-25 share-toggle 세션 기준).
> categories-v1 이후 신규 unit 2 추가됐을 가능성 있어 ±2 오차 허용.

- [ ] **Step 2: Manual QA 체크리스트 작성**

`docs/qa/phase8-pwa-checklist.md`:

```markdown
# Phase 8 PWA — Manual QA 체크리스트

## 1. Chrome (Desktop) — Application 탭 검증
- [ ] DevTools → Application → Manifest: name="travel-manager", icons 3종, theme #f2f1ed
- [ ] Application → Service Workers: `/sw.js` activated and is running, scope `/`
- [ ] Application → Cache Storage: `static-assets`, `pages` 캐시 채워짐
- [ ] Network 탭 → Disable cache 끄고 재방문: 정적 자산이 (from ServiceWorker) 표시
- [ ] Lighthouse → PWA: "Installable" + "PWA Optimized" 통과

## 2. Chrome — 오프라인 동작
- [ ] DevTools → Network → Offline 토글
- [ ] 이미 본 trip detail 페이지 새로고침 → 캐시 hit (HTML SWR + static CacheFirst)
- [ ] 새 trip URL 입력 → `/offline` 페이지 표시 (정적 fallback)
- [ ] "다시 시도" 버튼 클릭 → location.reload (Online 복원 후 정상 동작)

## 3. iOS Safari (실기기)
- [ ] Safari → 공유 → 홈화면에 추가 → 아이콘이 apple-touch-icon-180 사용
- [ ] 홈화면 아이콘 탭 → standalone (Safari URL bar 없음)
- [ ] 상태표시줄 색이 theme_color 와 비슷한 톤
- [ ] (참고) iOS 는 SW precache 정책이 Chrome 대비 보수적 — 단순 설치성만 검증

## 4. Android Chrome (실기기)
- [ ] 메뉴 → "앱 설치" 옵션 등장
- [ ] 설치 후 앱 드로어에서 실행 → standalone

## 5. CSP 위반 부재
- [ ] DevTools Console 에 CSP 위반 0건 (특히 `worker-src` 관련)
- [ ] Service Worker 등록 console.warn 0건

## 6. dev 모드 SW 비활성화
- [ ] `pnpm dev` 후 Application → Service Workers 비어있음
- [ ] HMR 정상 동작 (코드 수정 → 페이지 자동 갱신)

## 7. 성능 (참고)
- [ ] 재방문 시 first contentful paint 가 신규 방문 대비 단축 (Lighthouse 비교)
- [ ] Pretendard 폰트 woff2 가 Cache Storage hit
```

- [ ] **Step 3: spec 갱신**

`docs/specs/2026-04-20-travel-manager-design-updated.md` 에서:

(a) §0 Implementation Status 표 — `Phase 8` row 의 status 를 `⏳ Outstanding` → `✅ Complete` 로 변경, tag 컬럼에 `phase-8-pwa` 추가.

(b) §0.1 Spec ↔ Code 실재성 매핑 표 — `public/manifest.json, public/sw.js, Workbox` row 의 "부재" 를 다음으로 교체:

```
`public/manifest.webmanifest` (commit) + `public/sw.js` (build artifact) + `app/sw.ts` + `lib/pwa/runtime-caching.ts` + `components/pwa/service-worker-registrar.tsx`
```

비고 컬럼: `Phase 8 미구현` → `Phase 8 ✅ (tag phase-8-pwa)`

(c) §9 표 row 8: `⏳` → `✅`, tag `phase-8-pwa`. 비고에 "마이크로 인터랙션 폴리시는 별도 plan" 명시.

(d) §1 status frontmatter — `Phase 8 PWA + ...outstanding` 에서 `Phase 8 PWA` 제거, `Phase 8 PWA ✅` 변경 이력에 추가.

(e) "변경 이력" 표 맨 아래에 새 row:

```
| 2026-04-25 phase-8 | Phase 8 PWA 구현 — @serwist/next + manifest.webmanifest + 4 icons + app/sw.ts (Workbox runtime caching: Supabase/Maps NetworkOnly + 정적 CacheFirst + HTML SWR) + /offline fallback + ServiceWorkerRegistrar (production-only). plan: docs/plans/2026-04-25-phase8-pwa.md, tag phase-8-pwa |
```

- [ ] **Step 4: 위키 status.md 갱신**

`/Users/sohyun/Library/CloudStorage/SynologyDrive-home/앱/MY_AI_WIKI/projects/travel-manager/status.md` 의 "현재 단계" 블록 맨 아래에 row 추가:

```
✅ **Phase 8 PWA 완료** (2026-04-25) — `@serwist/next` 기반 Workbox SW + manifest + 4 icons + /offline fallback. Supabase/Maps NetworkOnly · 정적 CacheFirst · HTML SWR. dev 비활성화 (HMR 충돌). tag `phase-8-pwa`. plan: `docs/plans/2026-04-25-phase8-pwa.md`. 다음: §6.11 V2 커스텀 카테고리 OR Maps prod 도메인.
```

`다음 할 일` 섹션 → `Phase 8 PWA` 줄을 `[x]` 로 마크.

- [ ] **Step 5: 최종 commit + tag**

```bash
git add docs/specs/2026-04-20-travel-manager-design-updated.md docs/qa/phase8-pwa-checklist.md
git commit -m "$(cat <<'EOF'
docs(spec): mark Phase 8 PWA complete + add manual QA checklist

§0 Implementation Status / §0.1 mapping / §9 / 변경 이력 갱신.
QA 체크리스트 docs/qa/phase8-pwa-checklist.md 신설 (Chrome/iOS/Android
+ CSP + dev 비활성 검증 7 섹션).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

git tag phase-8-pwa
```

(위키 commit 은 별도 위크리/저장소이므로 사용자 수동 또는 wiki-end skill 로 처리.)

- [ ] **Step 6: Push 안내 (사용자 수동)**

```bash
echo "다음 명령은 사용자 PAT 필요 (yeoullab):"
echo "  git push origin main --follow-tags"
```

---

## Risks & Mitigations

| # | 위험 | 가능성 | 영향 | 완화 |
|---|---|---|---|---|
| R1 | `@serwist/next` 와 Next 16 호환 미검증 (peer 는 `>=14.0.0`) | 중 | high | Task 1 직후 build smoke (Task 3 step 4 의 의도적 fail 통과 + Task 4 step 6) — 첫 실패 시 next-pwa 또는 manual SW 로 롤백 plan |
| R2 | 사용자가 dev 에서 SW 가 캐시한 옛 코드를 보는 사고 | 낮 (disable=true) | mid | dev 비활성 정책 + Task 8 E2E 의 manifest/offline 만 검증, SW 동작은 production smoke 만 |
| R3 | iOS Safari 가 maskable 미지원 + Apple meta 누락 시 홈화면 아이콘 깨짐 | 중 | mid | Task 6 의 `apple-touch-icon-180` 명시 + manual QA §3 |
| R4 | Supabase REST 응답이 잘못 캐시돼 stale data → 사용자 혼동 | 낮 (NetworkOnly 명시) | high | Task 4 step 1 unit test 9 cases 가 Supabase URL → NetworkOnly 매칭 강제 |
| R5 | Workbox precache 가 빌드 해시 변경 시 자동 무효화 안 되면 옛 JS 박힘 | 낮 (`@serwist/next` 자동 처리) | high | `_next/static` URL 자체가 hash 포함 → CacheFirst 가 새 hash 를 새 entry 로 인식. Manual QA §1 의 재방문 검증 |
| R6 | CSP `worker-src 'self'` 누락 시 SW 등록 거부 | 낮 (Task 3 에서 추가) | mid | Manual QA §5 에서 console 에러 검증 |
| R7 | `public/sw.js` 가 commit 되어 main 에 빌드 산출물이 들어감 | 낮 | low | `.gitignore` Task 4 step 7 에서 명시 추가 |
| R8 | sharp 가 CI/Vercel build 환경에서 native binary 빌드 실패 | 낮 (sharp 는 Vercel 기본 지원) | mid | sharp 는 Task 1 에서만 install, build 시점엔 호출 안 됨 (icons:gen 은 dev 만). icon PNG 는 commit 됨 |

---

## Self-Review Note

**Spec coverage** ✓ — §0 / §0.1 / §1 / §2 / §3 / §9 의 PWA 항목 전부 Task 매핑 (Task 9 의 spec 갱신까지 포함).

**Placeholder scan** ✓ — 모든 Step 에 실제 코드/명령/예상 결과 포함. "TBD/TODO/적절히" 0건.

**Type/이름 일관성** ✓:
- `RUNTIME_CACHE_POLICIES` / `matchPolicy` / `RuntimeCachePolicy` 가 Task 4 (정의) ↔ Task 4 step 1 unit test 에서 동일.
- `ServiceWorkerRegistrar` 가 Task 7 (정의) ↔ providers.tsx (mount) 일관.
- manifest filename 은 `manifest.webmanifest` 로 통일 (Task 2 / Task 6 / Task 8 / Task 9).

**Out-of-scope** 명시:
- Background Sync, Push, IndexedDB queue → Risks R 외부 follow-up
- 마이크로 인터랙션 폴리시 → 별도 plan 후보 (Task 9 spec 갱신에서 명시)

---

## 다음 할 일 (이 plan 종료 후)

- [ ] §6.11 V2 커스텀 카테고리 (DB schema 확장 + RLS group-aware + CRUD RPC 4종 + dissolve fanout + UI 색상 picker)
- [ ] Maps prod 도메인 등록 (사용자 작업)
- [ ] **마이크로 인터랙션 폴리시** plan — DESIGN.md §10.x 의 transition/timing/spring 표준화. PWA 와 직교
- [ ] (선택) Background Sync — 오프라인 mutation 큐 + 온라인 복귀 시 재시도. Supabase 토큰 갱신과 충돌 검증 필요
- [ ] (선택) Workbox precache strategy 의 `_next/data` 캐시 추가 검토 (App Router RSC payload)
